//take in an array
const getRunningTotal = (array) => {
  //get each item in the array

  //get the unit price

  //check if there are any pricing rules for that item
  return array;
}

module.exports = {getRunningTotal}